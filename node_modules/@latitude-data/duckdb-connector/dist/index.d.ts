import { BaseConnector, CompiledQuery, ConnectorOptions, ResolvedParam, type BuildSupportedMethodsArgs } from '@latitude-data/source-manager';
import { type SupportedMethod } from '@latitude-data/sql-compiler';
import QueryResult from '@latitude-data/query_result';
export type ConnectionParams = {
    url?: string;
};
export default class DuckdbConnector extends BaseConnector<ConnectionParams> {
    private client?;
    private url;
    constructor(options: ConnectorOptions<ConnectionParams>);
    end(): Promise<void>;
    resolve(value: unknown): ResolvedParam;
    protected buildSupportedMethods(buildArgs: BuildSupportedMethodsArgs): Record<string, SupportedMethod>;
    private createClient;
    runQuery(compiledQuery: CompiledQuery): Promise<QueryResult>;
    private buildQueryParams;
}
//# sourceMappingURL=index.d.ts.map