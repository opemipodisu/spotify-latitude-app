import type { ReadNodeMetadataProps, ResolveNodeProps } from '../types';
import type { UpdateExpression } from 'estree';
/**
 * ### UpdateExpression
 * Represents a javascript update expression.
 * Depending on the operator, it can increment or decrement a value.
 * Depending on the position of the operator, the return value can be resolved before or after the operation.
 *
 * Examples: `{--foo}` `{bar++}`
 */
export declare function resolve({ node, scope, raiseError, ...props }: ResolveNodeProps<UpdateExpression>): Promise<unknown>;
export declare function readMetadata({ node, ...props }: ReadNodeMetadataProps<UpdateExpression>): Promise<import("../../types").QueryMetadata>;
//# sourceMappingURL=updateExpression.d.ts.map